---
import roadmap from "../data/roadmap.json";

const statusIcon = (status) => {
  if (status === "complete") return "✅";
  if (status === "in-progress") return "🚧";
  return "⬜";
};
---

<div class="max-w-3xl mx-auto px-6 py-12">
  <h1 class="text-4xl font-bold mb-4">Proficiency Roadmap</h1>
  <blockquote class="italic text-lg text-gray-600 border-l-4 border-gray-400 pl-4 mb-8">
    The way to kill imposter syndrome is with proof. Here’s mine.
  </blockquote>

  {roadmap.map((section) => (
    <div class="mb-10">
      <h2 class="text-2xl font-semibold mb-4">{section.category}</h2>
      <div class="grid gap-4">
        {section.items.map((item) => (
          <div class="p-4 rounded-2xl shadow bg-white flex flex-col md:flex-row justify-between items-start md:items-center">
            <div class="flex items-center gap-2">
              <span class="text-xl">{statusIcon(item.status)}</span>
              <span class="font-medium text-lg">{item.name}</span>
            </div>
            {item.repo ? (
              <a
                href={item.repo}
                target="_blank"
                class="mt-2 md:mt-0 inline-block bg-black text-white text-sm px-4 py-1 rounded-full hover:bg-gray-800"
              >
                View Repo
              </a>
            ) : null}
          </div>
        ))}
      </div>
    </div>
  ))}
</div>
