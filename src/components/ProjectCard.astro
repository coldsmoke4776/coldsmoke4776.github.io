---
const { title, description, github, demo } = Astro.props;
---

<div class="terminal-box p-4">
  <p class="command">$ cat {title.replace(/\s+/g, "_").toLowerCase()}.txt</p>
  <h2 class="job-title">{title}</h2>
  <p class="text-sm text-gray-400">{description}</p>

  {demo && (
    <div class="mt-2">
      <img src={demo} alt={`${title} demo`} class="rounded-lg border border-gray-700" />
    </div>
  )}

  <a href={github} target="_blank" class="text-accent-400 underline mt-2 block">
    ➜ View on GitHub
  </a>
</div>
